{#
/**
 * @file
 * Theme override to display a region.
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: HTML attributes for the region div.
 * - region: The name of the region variable as defined in the theme's
 *   .info.yml file.
 *
 * @see template_preprocess_region()
 */
#}
{% if content %}
  <div{{ attributes.addClass("wrapper-inner") }}>
    {{ content }}
    <p class="copyright-website">{{ "Copyright"|t }} © {{ current_year }} - {{ site_name }}<br> {{ "This is not an official website, it has been created by fans for fans of the actor Dan Payne."|t }}</p>
  </div>
{% endif %}